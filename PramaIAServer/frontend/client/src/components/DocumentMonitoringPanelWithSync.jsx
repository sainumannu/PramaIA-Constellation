import DocumentMonitoringPanel from './DocumentMonitoringPanel';

// Questo file è stato suddiviso per migliorare la manutenibilità.
// I contenuti sono stati spostati nei seguenti componenti:
// - DocumentMonitoringPanel.jsx (componente principale)
// - ClientsPanel.jsx (vista client)
// - EventsPanel.jsx (vista eventi)
// - PDFEventsDetailsPanel.jsx (vista dettagli eventi PDF)

// Utilizziamo il componente principale e lo esportiamo con lo stesso nome
// per mantenere la compatibilità con le importazioni esistenti
export default DocumentMonitoringPanel;
